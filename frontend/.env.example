# Frontend environment template (Next.js — NEXT_PUBLIC_* only).
# Local dev:  cp .env.example .env.local
# Production: cp .env.production .env.local  (or set vars before npm run build)

NEXT_PUBLIC_API_BASE_URL=http://localhost:8000
NEXT_PUBLIC_WS_BASE_URL=ws://localhost:8000
