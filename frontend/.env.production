# Production build — edit before `npm run build` on the server.
# deploy script reads this file for NEXT_PUBLIC_* at build time.

NEXT_PUBLIC_API_BASE_URL=https://your-server-domain
NEXT_PUBLIC_WS_BASE_URL=wss://your-server-domain
